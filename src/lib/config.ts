const config = {
  microcopy: {
    category: {
      defaultTitle: 'Category',
    },
    index: {
      defaultTitle: 'All Recipes',
    },
    recipe: {
      defaultTitle: 'Recipe',
    },
    tag: {
      defaultTitle: 'Tag',
    },
  },
  images: {
    aspectRatio: 0.75,
    responsiveIncrement: 400,
  },
};

export default config;
